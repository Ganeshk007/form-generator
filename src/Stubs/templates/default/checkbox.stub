<div class="form-check">
    <input class="form-check-input"
           type="checkbox"
           id="{{ $id }}"
           name="{{ $name }}"
           value="1"
           {{ $required }}>

    <label class="form-check-label" for="{{ $id }}">
        {{ $name }}
    </label>
</div>